# Dependabot version updates are disabled — bump dependencies manually in PRs.
# To re-enable, restore package-ecosystem entries under `updates`.
# Security alerts/updates are controlled separately under repo Settings → Code security.
version: 2
updates: []
