# apps/ui — project npm config
# Native bindings for Mac/Windows/Linux are installed by
# scripts/ensure-native-bindings.mjs (postinstall + npm run deps:native:all).
# Do not sync a Mac-only or Windows-only node_modules across OSes without
# re-running: npm run deps:native:all
